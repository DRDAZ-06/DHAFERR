# Environment Variables
# Copy this file to .env.local and fill in the values

# Admin password hash (generate using bcrypt)
# To generate: run this in Node.js console:
# const bcrypt = require('bcryptjs'); bcrypt.hash('your-password', 10).then(console.log);
ADMIN_PASSWORD_HASH=

# JWT Secret for session tokens (use a random string)
JWT_SECRET=your-secret-jwt-key-change-this-in-production
